@model RailConnect.Models.StanicaPolazakViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details StanicaPolazak</h1>

<div>
    <h4>StanicaPolazak</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Naziv)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Naziv)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.GradNaziv)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.GradNaziv)
        </dd>
    </dl>
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model?.IdStanicaPolazak" class="btn btn-warning" style="margin-top:5px;">Edit</a> 
    <a asp-action="Index" asp-route-gradId="@ViewBag.gradId" class="btn btn-primary" style="margin-top:5px;">Back to List</a>

</div>
